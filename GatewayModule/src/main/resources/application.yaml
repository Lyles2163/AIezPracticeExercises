server:
  port: 8100



jwt:
  # 开发环境默认值，生产必须用环境变量覆盖
  secret: ${JWT_SECRET:5p2O6Ziz6LaF57qn5peg5pWM5biFNjY2}
  expiration-ms: ${JWT_EXPIRATION_MS:43200000}

spring:
  cloud:
    gateway:
      routes:
        - id: user-service-route
          uri: lb://UserModuleServer
          predicates:
            - Path=/user/**

  config:
    import: nacos:gateway-module.yaml   # 改成你实际的 Data ID
    nacos:
      config:
        server-addr: 127.0.0.1:8848     # 改成你实际的 Nacos 地址